# 工具调用、续聊和 Arena 更新验收（2026-09-06）

后续深度检查与最终部署结果见 `docs/deep-audit-2026-09-06.md`：已自动重启、Arena 已登录，文本续聊和生图已实测通过；下面 832 项是前一阶段快照，最终扩展回归为 926 项。

## 已修复

| 项目 | 修复内容 | 验证 |
|---|---|---|
| 设置页工具测试失败 | 不再依赖默认未启用的管理 API；走受信任的应用 IPC，使用真实监听端口 | 隔离 Electron 在管理 API 关闭时仍能运行；无账号明确失败 |
| 工具测试误报 | 从“生成测试样例”改为真实两轮回显；校验调用名、唯一 ID、完整参数、工具结果及同一会话 | DeepSeek Flash 两轮 HTTP 200，完整通过 |
| 工具参数损坏 | 按 schema 保留字符串类型、JSON 形状的文件内容、代码空白和 CDATA | 解析、流式分段与 Anthropic 工具往返回归 |
| 工具流重复 ID/丢内容 | 每次工具调用使用唯一 ID；保留后续文本和下一段工具封装；代码围栏不触发工具 | 多次调用、拆块、四/五字符代码围栏回归 |
| 重复前置提示 | 新会话仅首次生成和注入系统/工具说明；续聊保留解析计划但只传新增消息 | 约 96 KB Claude Code 系统提示的首轮/工具回传测试 |
| Claude Code 会话识别 | 区分会话、子代理及父代理标识；保留旧 metadata 身份兼容 | 身份隔离与历史匹配测试 |
| 请求统计与取消 | 成功按完整 HTTP 输出计算；断连和不完整流只结算一次，并取消 Arena 上游 | 本机 HTTP 生命周期测试 |
| 开启鉴权但无可用密钥 | 默认拒绝请求，不意外开放服务 | 鉴权边界测试 |
| 旧版 completions 端点 | 输出真正的 text_completion JSON/SSE，不冒充 chat completion | 流式、非流式、echo 测试；不支持参数明确拒绝 |
| 图片生成误报完成 | 图片存在但终止原因为取消、过滤、未知等情况必须失败；只接受 stop 加完整流结束 | 实际生成方法的离线回归 |
| 测试进程占用过高 | 测试并发限制为 4，避免与构建同时运行时大批进程耗尽 Windows 内存 | 调整后全量回归通过 |

## 已添加

Arena 独立浏览器账号、实时文本/生图模型目录、聊天及续聊适配、流式解码、OpenAI 图片生成端点、账号界面和双语说明。仅保存应用自有浏览器资料 ID，不导入个人浏览器凭据。动态模型列表不会被重启或恢复默认覆盖，也不会由其他供应商兜底。

使用说明见 `docs/providers/arena.md`，Claude Code 与工具测试说明见 `docs/claude-code.md`。

## 本次验证证据

- `artifacts/tools-arena-tests.log`：832 项通过，0 失败，0 跳过（含可选原生浏览器发现检查）。
- `artifacts/tools-arena-tests-unbounded-oom.log`：保留限制并发前的内存/进程启动失败记录，未把该轮失败视为通过。
- `artifacts/tools-arena-build.log`：生产构建成功。
- `artifacts/tools-arena-runtime.log`：13 项隔离 Electron 检查通过，正式账号资料未参与隔离测试。
- `artifacts/proxy-tools-probe.json`：DeepSeek Flash 两轮真实工具请求通过。
- `artifacts/proxy-catalog-probe.json`：实际监听与设置均为 8081，健康及模型列表均 HTTP 200。
- Windows 可访问性可以读取应用，但模拟点击返回 `coordinate input geometry is unavailable`；因此按钮链路由实际 preload/IPC 运行验证，未把 UI 点击失败描述成成功。

## 尚待交互验收 / 兼容边界

- 已由 `restart-local.ps1` 自动重启加载最后修补，不再需要用户退出旧实例。
- Arena 登录与 Max 文本首轮/续聊、单张生图验收已通过；191 个当前目录项不等于所有模型均已逐个生成验证。
- Z.ai/GLM 实测仍要求网页验证码，未将其报告为可用。
- Arena 当前只支持单张文生图、URL、自动尺寸；没有图片编辑、批量、b64_json 或固定分辨率。
- Claude Code 为客户端工具兼容，并非 Anthropic 原生服务；不提供服务端工具、签名思考、严格 schema 或精确 token 计费。
- 网页会话索引当前保存在应用内存，重启后不会继续猜测之前的会话。模型、系统/工具定义变化或不完整回复需要显式新会话。
- 全量严格 TypeScript 检查仍存在项目原有类型问题；生产构建与测试通过，不宣称全项目类型检查已清零。
- 此次针对用户列出的问题和关联路径修复，不把“所有未知缺陷及所有缺失功能”视为已穷尽。
